# Heartbeat

If heartbeat is enabled, check whether the BLISS vault monitor is due. For vault processing, use the `bliss_vault_monitor` skill and run exactly:

```sh
npm run monitor:vaults
```

Keep summaries short. Do not include secrets, mnemonics, access tokens, or raw Supabase keys in output.
